{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "predeploy": "nuxt generate",
    "deploy": "gh-pages --nojekyll --cname patrickjparker.dev -d .output/public"
  },
  "dependencies": {
    "nuxt": "^3.12.3",
    "vue": "latest"
  },
  "devDependencies": {
    "@bg-dev/nuxt-naiveui": "^1.14.0",
    "@nuxtjs/tailwindcss": "^6.12.1",
    "@vicons/fa": "^0.12.0",
    "gh-pages": "^6.1.1"
  }
}
